# 🕒 XStopwatch — React Stopwatch Application

## 📌 Objective

Build a simple Stopwatch application in React with the ability to **start**, **stop**, and **reset** the timer.  
Time should display in **minutes:seconds** format and update every second when running.

---

## 🚀 Features

- Displays time in **mm:ss** format (initial: `0:00`)
- **Start** the stopwatch
- **Stop** the stopwatch
- **Reset** back to `0:00`
- Real-time updates with `setInterval`

---

## 📦 Installation

- npm install
- npm start
